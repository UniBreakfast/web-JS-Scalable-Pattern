'use strict';

  // application core is the main module - the conductor for all other modules in use

//debugger;


/*
  // data transporter module testing 0.1
var data = dt.load();
console.log(data);
data = dt.load2('headers', 'rows');
console.log(data);
data = dt.add_route('get_activities', 'activities', ['headers']).get_activities(['rows']);
console.log(data);
data = dt.use_route('get_activities', ['rows']);
console.log(data);
*/
